web:
  port: 8080

storage:
  type: sqlite
  path: /data/status.db

ui:
  title: "Lux Status"
  description: "Real-time network health and performance"
  header: "Status"
  logo: "/brands/lux/logo.svg"
  link: "https://lux.network"
  dark-mode: true
  favicon:
    default: "/brands/lux/favicon.svg"
    size16x16: "/brands/lux/favicon-16.png"
    size32x32: "/brands/lux/favicon-32.png"
  buttons:
    - name: "LIPs"
      link: "https://github.com/luxfi/lips"
    - name: "Docs"
      link: "https://docs.lux.network"
    - name: "GitHub"
      link: "https://github.com/luxfi"
    - name: "Support"
      link: "https://lux.network/support"

endpoints:

  # ═══════════════════════════════════════════════
  # MAINNET
  # ═══════════════════════════════════════════════

  - name: "API Gateway"
    group: "Mainnet"
    url: "https://api.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 404)"
      - "[RESPONSE_TIME] < 3000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Explorer (C-Chain)"
    group: "Mainnet"
    url: "https://explore.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 5000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Explorer Hanzo"
    group: "Mainnet"
    url: "https://explore-hanzo.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 5000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Explorer SPC"
    group: "Mainnet"
    url: "https://explore-spc.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 5000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Explorer Zoo"
    group: "Mainnet"
    url: "https://explore-zoo.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 5000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Explorer Pars"
    group: "Mainnet"
    url: "https://explore-pars.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 5000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Explorer API"
    group: "Mainnet"
    url: "https://api-explore.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302, 404)"
      - "[RESPONSE_TIME] < 5000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  # ═══════════════════════════════════════════════
  # SERVICES
  # ═══════════════════════════════════════════════

  - name: "Bridge"
    group: "Services"
    url: "https://bridge.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 5000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Exchange"
    group: "Services"
    url: "https://lux.exchange"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 5000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "MPC Wallet"
    group: "Services"
    url: "https://mpc.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 3000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Markets"
    group: "Services"
    url: "https://markets.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 3000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  # ═══════════════════════════════════════════════
  # CLOUD
  # ═══════════════════════════════════════════════

  - name: "Cloud"
    group: "Cloud"
    url: "https://cloud.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == any(200, 302)"
      - "[RESPONSE_TIME] < 3000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Cloud API"
    group: "Cloud"
    url: "https://api.cloud.lux.network/health"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 2000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "Cloud WebSocket"
    group: "Cloud"
    url: "wss://ws.cloud.lux.network"
    interval: 60s
    conditions:
      - "[CONNECTED] == true"

  # ═══════════════════════════════════════════════
  # WEB
  # ═══════════════════════════════════════════════

  - name: "lux.network"
    group: "Web"
    url: "https://lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 3000"
      - "[CERTIFICATE_EXPIRATION] > 720h"

  - name: "docs.lux.network"
    group: "Web"
    url: "https://docs.lux.network"
    interval: 60s
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 3000"
      - "[CERTIFICATE_EXPIRATION] > 720h"
