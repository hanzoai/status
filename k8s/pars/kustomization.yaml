apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pars-system

resources:
  - namespace.yaml
  - ../base
  - ingress.yaml

configMapGenerator:
  - name: status-config
    files:
      - config.yaml=../../config/pars.yaml

generatorOptions:
  disableNameSuffixHash: true
