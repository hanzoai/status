apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: status
  labels:
    app: status
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: ingress
  tls:
    - hosts:
        - status.pars.network
      secretName: status-pars-network-tls
  rules:
    - host: status.pars.network
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: status
                port:
                  number: 80
