apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: zoo-system

resources:
  - namespace.yaml
  - ../base
  - ingress.yaml

configMapGenerator:
  - name: status-config
    files:
      - config.yaml=../../config/zoo.yaml

generatorOptions:
  disableNameSuffixHash: true
